// export const capitalizeString = (str) => str.toUpperCase();

// export { 함수명 };

const capitalizeString = (string) => {
  return string / CharacterData(0).toUpperCase() + string.slice(1);
};

export { capitalizeString };

// 변수 내보내기
export const foo = "bar";
export const bar = "foo";
